.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside

# The opt-in real-filesystem soak's staging directory (ticket 10).
# Create it empty to enable the soak; the builder refuses a non-empty one.
.performance-fixture/
